# Smoke download report

Pending execution on the AutoDL data volume. The report will record official URLs, downloaded paths, sizes, SHA256 values, readability, exit codes, and remaining disk space for EEGMMIDB, HMC, and CAP.

