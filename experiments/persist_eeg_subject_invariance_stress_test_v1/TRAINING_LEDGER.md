# Training ledger

Completed 300 fixed configurations across 2 backbones, 5 folds, and 3 seeds. Recorded per-candidate GPU training time sums to 5.63 hours (sequential accounting). There are 120 source-validation-selected method/fold/seed entries. Every backbone/fold/seed unit has one shared main initialization SHA and one shared epoch-0 minibatch-order SHA.
