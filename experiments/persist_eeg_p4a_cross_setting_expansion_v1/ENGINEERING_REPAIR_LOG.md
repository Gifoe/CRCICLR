# Engineering Repair Log

Before outcome access, the initial staging implementation was repaired to use the exact P2/P3 subject-centroid SVD persistence construction and P3 full-space displacement-matched random controls. No dataset, role, seed, method, lambda, competence gate, or outcome rule changed.

- Before training proceeded, a NumPy read-only boolean-mask error was repaired by requesting a writable mask copy. This changed no scientific rule.
- The protocol-freeze recorder was made line-ending invariant for Windows Git checkout. Protocol content and its recorded SHA remained unchanged.
- During the Lean computational-scope pause, `Stop-ScheduledTask` stopped the scheduled parent but left its Python grid child alive. The child completed two additional S5 configurations after the provisional 203/405 snapshot. A source-completion-marker guard then allowed the active `S5/fold-2/seed-1/mmd__lambda-0.01` configuration to finish and terminated the orphan before another configuration was created. The final pause snapshot was regenerated at 205/405 (S4=135, S5=70, S6=0); the provisional snapshot was overwritten and is not used as evidence. No grid outcome was inspected for this decision.
- The first Lean validator invocation incorrectly required its own scheduled parent to be `Ready` while it was executing. This self-referential engineering condition was replaced by checks that the scheduled action is the Lean runner and the live non-ERM grid-process count is zero. No data, competence result, source primitive, or terminal rule changed.
