# P/C mechanism-closure analysis lock

Status: frozen design before the first full cell or outer-development mechanism evaluation. This lock supplements the user-supplied mechanism-closure prompt and the separately authorized `FINAL_PROJECTOR_AMENDMENT.md`; it does not modify upstream checkpoints, splits, normalizers, bases, Protected dimensions, or Phase 1 outputs. If any exact upstream hash, mapping identity, or P+C reconstruction fails, the affected cell is `FAIL_CLOSED` and cannot contribute to aggregate claims.

## Scope and evidence ladder

Exactly EEGNet and EEGConformer × OpenBMI MI and SSVEP × seed 0 × folds 0–4. No FBCNet, other seeds, final-heldout, new model, head fitting, P reselection, or outer-guided choice. The inferential unit is biological subject; trials only supply within-subject effect estimates. Evidence order is frozen final use → functional coupling → intervention mediation → directional specificity versus rank-matched random → training emergence/replay → subject/session drift → cross-architecture/task checks. A failed level narrows downstream claims; a debug probe never substitutes for a full cell.

## Frozen representations and interventions

Intermediate `Q_l^P` is fitted from TRAIN-only subject/session/class centroids to the frozen final canonical Protected coordinates using the same ridge mapping and singular-vector order as Phase 1. `P_l=(a_l-mu_l)Q_lQ_l.T`, `C_l=a_l-mu_l-P_l`. The classifier-input exception is the frozen canonical projector defined in `FINAL_PROJECTOR_AMENDMENT.md`, not its orthogonalized span. Each stage records projector mode, mapping hash, rank, and maximum per-trial reconstruction error; tolerance is `<1e-5`. At final, direct equality to the upstream canonical P is also required `<1e-5`.

Recipient/donor matching is fixed before outer evaluation: within the same biological subject and session, choose a donor of each other class by nearest activation norm, with stable tie-breaking by original row index. Recipients are a deterministic cap of 8 per subject/session/class, using a stable seed keyed by model, task, fold, stage, split and grouping key. TRAIN and outer-development use the same rule independently; outer labels may define evaluation-only class strata and margins, never a fitted mapping, top direction, epoch, or hyperparameter. Report cap coverage and missing eligible strata; do not replace absent pairs with cross-subject donors.

For every adjacent audited architecture stage, compute P swap, C swap, next-stage P/C hybrid outputs, centered-logit total and mediated effects, nonlinear residual, representation displacements, true/donor margin effects, and cosine to total. Norm ratios are descriptive, not additive percentages. Fixed long-range stages are EEGNet `(temporal_bn, spatial_elu_pool1, depth_point_elu_pool2)` and EEGConformer `(patch_tokenizer, conformer_block4, classifier_256_elu)`, each continued to frozen classifier input. These were selected by architecture position, not outcomes. A transition to final uses canonical final P/C; earlier transitions use orthogonal intermediate pathway P/C. “Causal mediation” describes interventions within a frozen computational network only, not causal attribution to human biology.

## Directional and random specificity

At intermediate stages, P axes are the ordered TRAIN mapping singular vectors. Final P axes are the frozen canonical raw vectors, individually normalized for physical perturbations and explicitly marked nonorthogonal; their coefficient SD is transformed consistently. C axes are label-free PCA of TRAIN complement activations. Primary `d_C=min(16,rank(C_l))`; sensitivity `min(32,rank(C_l))`, with rank threshold `max(1e-6*s_max,1e-8)`. Do not choose d_C from outer results. Coefficient SD is measured on TRAIN only, lower bounded by `1e-8` solely to detect a degenerate axis; degenerate axes are reported and not silently replaced.

Primary symmetric mixed finite difference uses `epsilon=0.5` TRAIN SD on each selected P and C axis. `epsilon=0.25` and `1.0` are sensitivity analyses, never tuned or pooled as a best case. The full TRAIN P×C matrix is ranked by mean centered-logit interaction norm across the deterministic capped TRAIN trials. Freeze the top 10 pairs per stage with lexicographic index tie-break; outer-development evaluates only those pairs and all preregistered aggregate controls. Report train rank, outer norm/margin effect, sign agreement, error association, and mass/concentration; role names are TRAIN-defined descriptive associations, not causal utility.

Use the first 20 of the previously locked equal-rank random final subsets, with their original draw IDs and SHA, not new favorable draws. Derive corresponding TRAIN-only layer pathways and their own random-core/random-complement matrices under identical cap, PCA dimensions, epsilon and top-10 selection rules. Compare maximum interaction, top-10 mass, concentration ratio, mediated effects and subject-level outer error association to the 20-draw distribution. Protected specificity requires superiority to the random distribution; if not, classify it as unresolved/generic. No trial-level p-value is a scientific conclusion.

## Training and subject statistics

Historical selected and last `latest.pt` endpoints are available and identity-audited in all 20 cells. Intermediate historical 10/25/50/75% checkpoints are absent. Reproduce the original TRAIN/validation recipe exactly and save each epoch in an independent replay directory, preserving all original training evidence. The fixed analysis schedule is 10%, 25%, 50%, 75%, best legal epoch and final epoch (deduplicate repeats), with epoch rounding fixed to nearest integer and ties upward, clamped to `[1,final_epoch]`. Compare replay best versus historical best on TRAIN/validation metric, outer-development BA for evaluation only, logits correlation, and parameter/checkpoint metadata. Unless exact identity is demonstrated, label `RETRAINED_REPLICA_TRAJECTORY` and restrict any time-course statement to the replica. Per subject/session/class checkpoint trial cap is 8, deterministic and fixed. At each checkpoint, rerun checkpoint-native PERSIST on TRAIN only; keep `P_t` separate from the frozen final-P backtrace. Outer-development checkpoint BA, macro-F1 and CE are descriptive, never selection criteria.

Aggregate every trial measure within biological subject and session first. Use 2000 deterministic subject-bootstrap resamples for 95% percentile confidence intervals, resampling biological subject IDs rather than trials; display subject count, fold support, undefined metrics and missing paired sessions. Fold consistency is assessed after per-subject aggregation. Same-subject session drift includes cosine, TRAIN-standardized Euclidean and component-wise signature differences, compared descriptively with P-only and C-only drift and future BA/CE/error using Pearson and Spearman with subject-bootstrap intervals. Optional regime clustering is secondary: TRAIN-only standardization, TRAIN PCA to 90% variance capped at 8, `K∈{2,3,4}` chosen by TRAIN silhouette, frozen centroids for outer assignment; skip with explicit reason if subject count or sessions are insufficient.

Cross-model depth profiles use fixed linear interpolation on `{0,0.25,0.5,0.75,1}` and report Spearman, cosine and sign agreement separately by task. Cross-model subject/trial joins require exact subject/session/trial identity; if identity is unverifiable, report the join failure rather than approximate matching. A `CROSS_MODEL_CROSS_TASK_PATTERN` requires same direction across all four model/task strata and a majority of folds in each; otherwise use only the user-specified task-specific, architecture-specific, mixed or unresolved categories. The final case classification must be evidence-bound and may remain unresolved. Do not design a performance model in this experiment.

## Required artifacts and no-fabrication gate

Produce all 15 artifacts named in the attachment, plus compact hash/provenance manifests. Every CSV row identifies model/task/fold, split/scope, projector mode where relevant, biological-subject aggregation level, and upstream protocol/implementation hashes. No result value is filled from a probe, failed cell, invalid original AUROC, or nonidentical replay presented as history. Only reviewed compact outputs and hashes are eligible for GitHub push; full caches/checkpoints/logs remain on the original server.
