# Stage-1 protocol note

Same episode manifests: true. Same optimizer/hyperparameters: true. Same early-stopping rule: true. Same actual selected optimization steps: false because the models selected different epochs.
