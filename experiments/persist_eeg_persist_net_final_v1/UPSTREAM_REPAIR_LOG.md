# Upstream repair log

No upstream scientific definition has been changed.  The experiment uses the
Signed-V3.1 stable SHA256 seed principle and the exact DDA centered-logit
formula.  Engineering repairs, if any, will be appended with affected runs and
rerun scope.  An empty log means `none`, not an omitted audit.
