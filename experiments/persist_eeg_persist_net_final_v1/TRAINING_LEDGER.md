# Training ledger

Pending server execution.  The runner appends one row per fold/seed/model with
source roles, normalization provenance, architecture, epoch, parameter count,
checkpoint SHA256, target adaptation recipe, timing, and explicit future/outer
absence assertions.
