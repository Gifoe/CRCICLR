# LiteBN C/M optimizer-aligned harm audit

This branch audits C, M, and CM identity residual parameter spaces. It does not train a candidate or access development outer, benchmark, heldout, or sealed data. Run `code/run_optimizer_aligned_audit.py`, then `code/aggregate_optimizer_aligned_audit.py`.
