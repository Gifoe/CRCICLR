# Bug repair ledger

No scientific-rule repair is permitted. Engineering implementation uses the existing frozen source/refit loader, previous legal tau rows, deterministic sentinel-filtered sampler, exact AdamW displacement accounting, CPU accumulation for bounded memory, subject-cluster bootstrap, certificate-specific CE/BBR calibration and random controls, and certificate-specific gate bookkeeping.
