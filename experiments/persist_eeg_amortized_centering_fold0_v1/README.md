# Amortized centering, OpenBMI fold0

One seed-0 SEARCH-only screen comparing a locked matched EEGNet-ERM reference,
Decomp-CE, and Decomp-Offset.  The legal Decomp methods predict from a single
trial and never receive a target subject/session identifier or target history.

The prior PRD experiment's frozen fold-0 episode stream and normalization are
reused.  The source branch carries an explicit split-SHA amendment authorized
by the user; this experiment preserves that amendment rather than asserting an
unavailable historical SHA match.
