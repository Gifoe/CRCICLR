# PERSIST-EEG backbone generality seed-0

This is a frozen SEARCH-only screen of EEGConformer, CBraMod, and CodeBrain with the fixed RAW_LOGIT50 LiteBN and EEGNet controls. Runtime checkpoints remain outside git. The separate `--phase holdout` command is post-screen only and cannot affect selection.
