# Exp4 V2 model card

Terminal state: **EXP4_V2_NO_DEPLOYMENT_MATCHED_PROTECTED_DIRECTION**. The model uses an S1-only EEGNet anchor and a zero-initialized 32×32 residual adapter. The proposed constraint, when run, is a differentiable hinge on anchor-space intervention utility; it does not freeze coordinates. Outer subjects were not accessed.
