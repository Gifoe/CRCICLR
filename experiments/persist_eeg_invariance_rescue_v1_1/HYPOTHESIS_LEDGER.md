# Hypothesis ledger

H1: a controlled invariance objective can reduce cross-session subject
identity. H2: any Protected functional loss must exceed ordinary independent
retraining and matched non-Protected loss. H3: if H1/H2/task harm all hold,
the Protected residual should restore more task performance than rank-matched
generic residuals. Every hypothesis is allowed to fail; no family is selected
after outcome inspection.
