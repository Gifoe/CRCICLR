# Source development report

The bounded search contains 12 recipes (rank 1/2/4, `lambda_R` 1e-3/1e-2,
`lambda_P` 0.5/1.0), five folds, and three seeds.  Selection uses source
validation only; outcome rows are reported after selection.  The canonical
gate is the machine-readable `results/SOURCE_GATE.json` and is not relaxed for
additional-backbone exploration.

