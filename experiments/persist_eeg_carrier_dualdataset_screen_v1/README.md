# PERSIST-EEG carrier dual-dataset screen

This directory contains a resumable, SEARCH-only seed-0/fold-0 screen. Runtime checkpoints and GPU-resident cache are outside Git. Results are descriptive development results, not sealed confirmation.
