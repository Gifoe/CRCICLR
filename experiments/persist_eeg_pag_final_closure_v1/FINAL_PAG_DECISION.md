# Final PAG decision

## Terminal state

`PAG_PARTIAL_CONFIRMATION`. The development evidence supports the existence of persistent structure, task consequence, and bounded shared geometry, but the one-shot sealed confirmation could not run because the authorized OpenBMI internal holdout and WBCIC outer-10 resources are not materialized in the server repository.

The sealed runner returned `SEALED_CONFIRMATION_NOT_RUN_RESOURCE_UNAVAILABLE` with `outcome_labels_read=false` and `post_unblinding_repair=false`. This is a resource block, not a negative sealed outcome.

## Primary constructive result

The frozen primary method was PUD-Aux against an exactly matched task-only control with a +0.5 percentage-point BA meaningful-gain gate. The historical unpaired development comparison was `-0.9333 pp` (95% CI `-1.7250, -0.1333` pp). The exact matched development comparison was `0.4250 pp` (95% CI `-0.0000, 0.8167` pp), so it does not exclude zero or the preregistered +0.5 pp gate. These are separate comparisons and are not pooled.

## Strongest defensible claim

In frozen development protocols, persistent structure, task consequence, and bounded shared geometry are observable, but they do not certify a reliable unseen-subject constructive intervention; independent sealed confirmation remains incomplete because the authorized resources are unavailable in the server repository.

`READY_FOR_ICLR_MANUSCRIPT = NO` for a confirmatory PAG manuscript until the missing independent sealed evidence is supplied and audited.
