# Method fidelity

V1.2 imports the exact Signed-V3.1 checkpoints and canonical spectra and
reuses V1.1 fixed control membership by hash. It changes only continuous
identity calibration, train-only noise-floor estimation, isotonic fitting, and
alpha interpolation. All task consequence code remains the V1.1
intervention-specific refit Ridge/BA setup. Outer data are never loaded.
