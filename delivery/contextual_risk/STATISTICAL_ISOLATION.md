# Statistical isolation

A/B selection used only `method_development`. `formal_calibration` and `internal_final_evaluation` were inaccessible to screening. All source-head seeds share one subject partition and one hash-derived five-fold rotation. Primary selection uses alpha=0.10 and delta=0.10.
