# Input and leakage audit

Resolved all required inputs. Method-development subjects: {'eegmmidb': 65, 'hmc': 90}. Only these subjects are eligible for sequence cache generation. All 50 source heads are frozen and their checkpoint hashes match. Processed labels align exactly with frozen token embeddings. Formal/internal/CAP cohorts are excluded by the master cohort filter.
