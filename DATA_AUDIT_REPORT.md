# Data audit report

This report is generated from `data/manifests/dataset_audit.json` after verified downloads. No official subject count is treated as the observed eligible count. Read failures, missing channels/runs, invalid annotations, duplicates, and every exclusion reason are retained in manifests.

